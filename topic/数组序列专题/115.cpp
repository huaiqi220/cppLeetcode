
#include <string>
#include <vector>

class Solution {
public:
  /*  int numDistinct(std::string s, std::string t) {
        int count = 0;

    }

    void backTracking(int& count, std::string s, std::string t,int idx, std::vector<int> slist) {
        if (idx == t.size()) {
            count++;
            return;
        }
        for (auto item : slist) {
            std::vector<int> list = searchCharFromIndex(s, item, t[idx])
        }
        
    }

    std::vector<int> searchCharFromIndex(std::string& str, int idx, char c)
    {
        std::vector<int> res;
        for (int i = idx; i < str.size(); i++)
        {
            if (str[i] == c)res.push_back(i);
        }
        return res;
    }*/
};